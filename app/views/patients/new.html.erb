<h2> Patient Registration Page </h2>

<%= form_with scope: :patient, url: patients_path, local: true, data: { turbo: false } do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    </br>
    <p>
    <%= f.submit %>
    </p>
<% end %>